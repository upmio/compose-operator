# This kustomization.yaml is not intended to be run by itself,
# since it depends on service name and namespace that are out of this kustomize package.
# It should be run by config/default
resources:
- bases/upm.syntropycloud.io_mysqlreplications.yaml
- bases/upm.syntropycloud.io_redisclusters.yaml
- bases/upm.syntropycloud.io_redisreplications.yaml
- bases/upm.syntropycloud.io_proxysqlsyncs.yaml
- bases/upm.syntropycloud.io_postgresreplications.yaml
- bases/upm.syntropycloud.io_mysqlgroupreplications.yaml
- bases/upm.syntropycloud.io_mongodbreplicasets.yaml
#+kubebuilder:scaffold:crdkustomizeresource

patches:
# [WEBHOOK] To enable webhook, uncomment all the sections with [WEBHOOK] prefix.
# patches here are for enabling the conversion webhook for each CRD
- path: patches/webhook_in_mysqlreplications.yaml
- path: patches/webhook_in_redisclusters.yaml
- path: patches/webhook_in_redisreplications.yaml
- path: patches/webhook_in_proxysqlsyncs.yaml
- path: patches/webhook_in_postgresreplications.yaml
- path: patches/webhook_in_postgresreplications.yaml
- path: patches/webhook_in_mongodbreplicasets.yaml
#+kubebuilder:scaffold:crdkustomizewebhookpatch

# [CERTMANAGER] To enable cert-manager, uncomment all the sections with [CERTMANAGER] prefix.
# patches here are for enabling the CA injection for each CRD
#- path: patches/cainjection_in_mysqlreplications.yaml
#- path: patches/cainjection_in_mysqlgroupreplications.yaml
#- path: patches/cainjection_in_redisclusters.yaml
#- path: patches/cainjection_in_redisreplications.yaml
#- path: patches/cainjection_in_proxysqlsyncs.yaml
#- path: patches/cainjection_in_postgresreplications.yaml
#- path: patches/cainjection_in_mongodbreplicasets.yaml
#+kubebuilder:scaffold:crdkustomizecainjectionpatch

# [WEBHOOK] To enable webhook, uncomment the following section
# the following config is for teaching kustomize how to do kustomization for CRDs.

configurations:
- kustomizeconfig.yaml
