apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.0
  creationTimestamp: null
  name: mysqlgroupreplications.upm.syntropycloud.io
spec:
  group: upm.syntropycloud.io
  names:
    kind: MysqlGroupReplication
    listKind: MysqlGroupReplicationList
    plural: mysqlgroupreplications
    shortNames:
    - mgr
    singular: mysqlgroupreplication
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.ready
      name: READY
      type: boolean
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: MysqlGroupReplication is the Schema for the Mysql Group Replication
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Defines the desired state of the MysqlGroupReplication.
            properties:
              aesSecret:
                description: AESSecret is the reference to the secret resource containing
                  aes key, it must be in the same namespace as the MysqlGroupReplication
                  Object.
                properties:
                  key:
                    default: AES_SECRET_KEY
                    description: Key is the key of the secret, which contains the
                      value used to decrypt aes key
                    type: string
                  name:
                    default: aes-secret-key
                    description: Name is the name of the secret resource which store
                      aes key.
                    type: string
                required:
                - key
                - name
                type: object
              member:
                description: Member is a list of nodes in the MySQL Group Replication
                  topology.
                items:
                  description: CommonNode information for node to connect
                  properties:
                    host:
                      description: Host specifies the ip or hostname of node
                      type: string
                    name:
                      description: Name specifies the identifier of node
                      type: string
                    port:
                      description: Port specifies the port of node
                      type: integer
                  required:
                  - host
                  - name
                  - port
                  type: object
                type: array
              secret:
                description: Secret is the reference to the secret resource containing
                  authentication information, it must be in the same namespace as
                  the MysqlGroupReplication object.
                properties:
                  mysql:
                    default: mysql
                    description: ' Mysql is the key of the secret, which contains
                      the value used to connect to MySQL.'
                    type: string
                  name:
                    description: Name is the name of the secret resource which store
                      authentication information for MySQL.
                    type: string
                  replication:
                    default: replication
                    description: Replication is the key of the secret, which contains
                      the value used to set up MySQL Group Replication.
                    type: string
                required:
                - mysql
                - name
                - replication
                type: object
            required:
            - member
            - secret
            type: object
          status:
            description: Populated by the system, it represents the current information
              about the MysqlGroupReplication.
            properties:
              conditions:
                description: |-
                  Represents a list of detailed status of the MysqlGroupReplication object.
                  Each condition in the list provides real-time information about certain aspect of the MysqlGroupReplication object.

                  This field is crucial for administrators and developers to monitor and respond to changes within the MysqlGroupReplication.
                  It provides a history of state transitions and a snapshot of the current state that can be used for
                  automated logic or direct inspection.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              observedGeneration:
                description: ObservedGeneration The generation observed by controller.
                format: int64
                type: integer
              ready:
                description: Ready indicates whether this MysqlGroupReplication object
                  is ready or not.
                type: boolean
              topology:
                additionalProperties:
                  properties:
                    gtidExecuted:
                      description: GtidExecuted indicates the gtid_executed of the
                        MySQL node.
                      type: string
                    host:
                      description: Host indicates the host of the MySQL node.
                      type: string
                    memberState:
                      description: MemberState indicates the member_state of the MySQL
                        node.
                      type: string
                    port:
                      description: Port indicates the port of the MySQL node.
                      type: integer
                    readonly:
                      description: ReadOnly specifies whether the node is read-only.
                      type: boolean
                    role:
                      description: Role represents the role of the node in the group
                        replication topology (e.g., primary, secondary).
                      type: string
                    status:
                      description: Ready indicates whether the node is ready for reads
                        and writes.
                      type: string
                    superReadonly:
                      description: SuperReadOnly specifies whether the node is super-read-only
                        (i.e., cannot even write to its own database).
                      type: boolean
                  required:
                  - gtidExecuted
                  - host
                  - memberState
                  - port
                  - readonly
                  - role
                  - status
                  - superReadonly
                  type: object
                description: Topology indicates the current MySQL Group Replication
                  topology.
                type: object
            required:
            - observedGeneration
            - ready
            - topology
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: null
  storedVersions: null
